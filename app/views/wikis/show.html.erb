<h1><%= @wikis.title %></h1>

<div class="row">
  <div class="col-8-md">
    <h5><%= @wikis.body %></h5>
    <small>
      Created by: <%=  @wikis.user.name %> <%= time_ago_in_words(@wikis.created_at) %> ago,
    </small>
    <% if policy(@wikis).edit? %>
      <%= link_to "Edit Wiki", edit_wiki_path, class: 'btn btn-success' %>
    <% end %>
    <% if policy(@wikis).destroy? %>
      <%= link_to "Delete Wiki", @wiki, method: :delete, class: 'btn btn-danger', data: {confirm: 'Are you sure you want to delete this Wiki?'} %>
    <% end %>
  </div>
</div>
